i want to include a minor bugfix which i just encountered (using the latest version);

- to get around the terminal flicker behaviour while i extended the blur & grain on terminal to under the system statusbar at the top i had made a sort of fix, where now 2 methods of drawing exists depending if i have some combination of blur/grain/monet overlay color then its one method of applicstion and with all of them turned off, it uses a fallback legacy fallback behavior tracking with uostream termux-app.
- currently when i turn everything off (blur/grain/monet overlay color) -> the extra padding bugwhere extra padding/space exists between the android softkeyboard and extrakeys bar (this is a bug i remedied by aligning with upstream termuxbehavior around this section) -> but its get completly fixed if i run "termux-reload-settings" in the terminal. -> so my question is, if we can tie in the "termux-reload-settings" behavior when users disable blur/grain/monet theme color overlay entirely to provide a smoother user experiance?

---

we are going to improve the app launching process, the idea is;

- when users touch and slide over the alphabets bar, it will have a bright monet colored real bloom (glowing orb) as a cursor indicating their touch surface.
- we naintain the current implimentation of live apps filtering (along with grouping them near the respective letters, slide etc).
- in addition, to cover for an edge case and for a smoother user experiance: when there are more apps than it fits the first page of apps bar while the filter is active -> i need the right edge of the apps bar to glow using a differrnt bright monet color variant to indicate to uses that there is another page and they are able to swipe there. the animation should be very smooth and soothing with reasonable physics emulation.
- in addition, i need to add another method of launching apps: when the user slides with the alphabets row and the apps start being filterd and the glowing orb cursor is showing -> once user is at a particular alphabet and the apps bar is in a filtered state -> the users should be able to move their finger eithout lifting it off the screen upwards from the alphabets row to the apps row -> the glowing orb then retains a small portion of it in the alphabet which was active & extends to wrap each app icon individually as the user moves their finger upto the apps bar and is sliding throgh the apps -> if there are more than one page of filtered apps, then when user slides their finfer to the glowing edge area, the appsbar should automatically scroll to next/previous page -> as the users are sliding from one app icon to next app icon, the glowing orb cursor should show physical weighted animations which are still smooth and soothing indicsting the current app icon being focussed -> once the user lifts the finger from the screen, the app that was focused at the time should launch with the glowing orb cursor grows within the entire apps-bar+alphabets-row+extrakeys-row and disappears.

---

commit & push this versionto dev & fastrack to dev, after that: lets rework the second page. we will switch its layout: it will be into a 2 rows. 1sr row will have 2 long vertical rectangle frames -> first column will have both the "colors" and "status bar sections" peoperly spaced as 2 paragraphs + second column will have the portrait wallpaper. the second row will span the entire length of the tui where the generated colors arealigned as a smooth gradiant line (so no abruptlt changine squa$e tiles as of now - instead a continous line soanning from left to right edge smoothly transitioning between colors), it can be one or two lines depending on the extracted colors, the aim is to get rid of the uncertainity on line length, this way i am hoping we can always create 2 thick lines which encompasses all the generated colors if there are more ot less colors it just means that there will be mor eor less transitions within those 2 lines (2 think lines stacked on top of each other) + we use this new line as the progress bar indicator (remove the one that appears at the top) thereby giving users a coherent visually pleasing experiance as the colors are generated. you can use of gum framework extensively to achieve the most visually smooth, physics based animations using charmbracelet's harmonica library

---
