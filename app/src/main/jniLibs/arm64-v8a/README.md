Native libraries for arm64-v8a (lite build).

## launcher_perf

`liblauncher_perf.so` is **not checked in**. Build from source:

```bash
./native/launcher-perf/build-android.sh
```

CI runs that script before packaging. MNN / LiteRT natives were removed for the lite APK cut.
